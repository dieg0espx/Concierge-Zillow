# BMM Workflow Status
# This tracks progress through BMM methodology phases
# Phase 2 (Implementation) is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/tech-spec.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2025-12-10"
project: "Concierge-Zillow"
project_type: "software"
selected_track: "quick-flow"
field_type: "brownfield"
workflow_path: "quick-flow-brownfield.yaml"

workflow_status:
  # Prerequisites (Brownfield)
  document-project: required

  # Phase 0: Discovery (Optional) - SKIPPED
  brainstorm-project: skipped
  research: skipped

  # Phase 1: Planning
  tech-spec: required

  # Phase 2: Implementation
  sprint-planning: required
